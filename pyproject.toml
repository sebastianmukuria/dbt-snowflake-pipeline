[project]
name = "data-pipeline"
version = "0.1.0"
description = "Coffee shop analytics pipeline: dbt + Snowflake. Includes a reproducible seed-data generator."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
